# Production Environment Variables
# Diese Datei NICHT committen (.gitignore)!
# Nur für lokales Production-Testing (npm run build && npm run start)

NODE_ENV=production

# MongoDB
MONGO_URI=deine-mongodb-uri-hier

# Google OAuth
GOOGLE_CLIENT_ID=deine-google-client-id
GOOGLE_CLIENT_SECRET=dein-google-client-secret

# Sessions & JWT
SESSION_SECRET=session-secret-min-32-zeichen
JWT_SECRET=jwt-secret-min-32-zeichen

# Frontend URL (wird deine Azure URL sein)
FRONTEND_URL=https://deine-app.azurewebsites.net

# Port (Azure setzt automatisch process.env.PORT)
PORT=8080

# VITE_API_URL wird NICHT gesetzt, damit relative URLs verwendet werden
