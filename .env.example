# MongoDB Connection
# Ersetzen Sie 'username' und 'password' mit Ihren MongoDB Atlas Credentials
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/?appName=YourApp

# Google OAuth Configuration
# Erstellen Sie diese in der Google Cloud Console (siehe README.md)
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-your-client-secret

# Google Callback URL
# LOKAL: http://localhost:8080/api/auth/google/callback
# PRODUCTION: https://ihre-app.azurewebsites.net/api/auth/google/callback
GOOGLE_CALLBACK_URL=http://localhost:8080/api/auth/google/callback

# Session & JWT Secrets
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=generate-a-random-secret-at-least-32-characters
JWT_SECRET=generate-a-random-secret-at-least-32-characters

# Server Configuration
PORT=8080

# Environment: development oder production
NODE_ENV=development

# Frontend URL (for CORS)
# LOKAL: http://localhost:5174
# PRODUCTION: https://ihre-app.azurewebsites.net
FRONTEND_URL=http://localhost:5174
